[pytest]
# Run unit tests by default, skip integration tests
# Use pytest -m integration to run integration tests
markers =
    integration: marks tests requiring GPU/voxcpm (deselect with '-m "not integration"')

[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*